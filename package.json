{
  "dependencies": {
    "vite": "^5.2.2",
    "vite-bundle-visualizer": "^1.1.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build && (npm run dev & lighthouse http://localhost:5173 --output html --output-path ./report.html)",
    "preview": "vite preview"
  }
}
